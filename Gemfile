source "https://rubygems.org"

# Use the same version that GitHub Pages uses
gem "github-pages", group: :jekyll_plugins

# Minimal additional dependencies for local development
gem "webrick", "~> 1.7"